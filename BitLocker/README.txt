BitLockerKey_EmailExport.ps1:
    This script is to run once a month and export all BitLocker keys via email.

BitLocker_CountByOU.ps1:
    This script just looks through Active Directory for a count of all the BitLocker keys, then returns a table of the total number of keys by OU.
